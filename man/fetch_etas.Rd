% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fetch_etas.R
\name{fetch_etas}
\alias{fetch_etas}
\title{Extract OMEGA (ETA) Estimates from a NONMEM Listing File}
\usage{
fetch_etas(lst, digits = NA, shk_digits = NA)
}
\arguments{
\item{lst}{An object of class \code{"lst"} from \code{\link[=read_lst_file]{read_lst_file()}}.}

\item{digits}{Integer or \code{NA}. Rounding for \code{rse}. Default \code{NA}.}

\item{shk_digits}{Integer or \code{NA}. Rounding for \code{shrinkage}. Default \code{NA}.}
}
\value{
A \code{\link[tibble:tibble]{tibble}} with columns:
\describe{
\item{parameter}{Character. Parameter label (e.g. \code{"ETA1"}).}
\item{estimate}{Numeric. Diagonal variance estimate.}
\item{se}{Numeric. Standard error. \code{NA} if no covariance step.}
\item{rse}{Numeric. Relative standard error (\%). \code{NA} if no covariance step.}
\item{shrinkage}{Numeric. ETA shrinkage (\%). \code{NA} if not reported.}
}
}
\description{
Parses the OMEGA covariance matrix diagonal (variance of random effects)
and, if available, standard errors, RSE, and ETA shrinkage from a NONMEM
\code{.lst} file.
}
\examples{
path <- system.file("testdata", "full_cov.lst", package = "lstparseR")
lst  <- read_lst_file(path)
fetch_etas(lst)
}
