% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fetch_sigmas.R
\name{fetch_sigmas}
\alias{fetch_sigmas}
\title{Extract SIGMA (EPS) Estimates from a NONMEM Listing File}
\usage{
fetch_sigmas(lst, digits = NA)
}
\arguments{
\item{lst}{An object of class \code{"lst"} from \code{\link[=read_lst_file]{read_lst_file()}}.}

\item{digits}{Integer or \code{NA}. Rounding for \code{rse}. Default \code{NA}.}
}
\value{
A \code{\link[tibble:tibble]{tibble}} with columns:
\describe{
\item{parameter}{Character. Parameter label (e.g. \code{"EPS1"}).}
\item{estimate}{Numeric. Diagonal variance estimate.}
\item{se}{Numeric. Standard error. \code{NA} if no covariance step.}
\item{rse}{Numeric. Relative standard error (\%). \code{NA} if no covariance step.}
}
}
\description{
Parses the SIGMA covariance matrix diagonal (variance of residual errors)
and, if available, standard errors and RSE from a NONMEM \code{.lst} file.
}
\examples{
path <- system.file("testdata", "full_cov.lst", package = "lstparsR")
lst  <- read_lst_file(path)
fetch_sigmas(lst)
}
