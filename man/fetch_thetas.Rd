% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fetch_thetas.R
\name{fetch_thetas}
\alias{fetch_thetas}
\title{Extract THETA Estimates from a NONMEM Listing File}
\usage{
fetch_thetas(lst, digits = NA)
}
\arguments{
\item{lst}{An object of class \code{"lst"} from \code{\link[=read_lst_file]{read_lst_file()}}.}

\item{digits}{Integer or \code{NA}. Number of decimal places to round \code{RSE}.
Default \code{NA} (no rounding).}
}
\value{
A \code{\link[tibble:tibble]{tibble}} with columns:
\describe{
\item{parameter}{Character. Parameter label (e.g. \code{"TH_1"}).}
\item{estimate}{Numeric. Point estimate.}
\item{se}{Numeric. Standard error. \code{NA} if no covariance step.}
\item{rse}{Numeric. Relative standard error (\%). \code{NA} if no covariance step.}
}
}
\description{
Parses the THETA (fixed effects) parameter estimates and, if available,
their standard errors and relative standard errors (RSE) from a NONMEM
\code{.lst} file.
}
\examples{
path <- system.file("testdata", "full_cov.lst", package = "lstparsR")
lst  <- read_lst_file(path)
fetch_thetas(lst)
}
