% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fetch_all.R
\name{fetch_all}
\alias{fetch_all}
\title{Fetch All Parameters from a NONMEM Listing File}
\usage{
fetch_all(lst, digits = NA, shk_digits = NA, ofv_digits = NA, cn_digits = NA)
}
\arguments{
\item{lst}{An object of class \code{"lst"} from \code{\link[=read_lst_file]{read_lst_file()}}.}

\item{digits}{Integer or \code{NA}. Rounding for RSE values. Default \code{NA}.}

\item{shk_digits}{Integer or \code{NA}. Rounding for ETA shrinkage. Default \code{NA}.}

\item{ofv_digits}{Integer or \code{NA}. Rounding for OFV. Default \code{NA}.}

\item{cn_digits}{Integer or \code{NA}. Rounding for condition number. Default \code{NA}.}
}
\value{
A named list with elements:
\describe{
\item{thetas}{\link[tibble:tibble]{tibble} from \code{\link[=fetch_thetas]{fetch_thetas()}}, or \code{NULL} on failure.}
\item{etas}{\link[tibble:tibble]{tibble} from \code{\link[=fetch_etas]{fetch_etas()}}, or \code{NULL} on failure.}
\item{sigmas}{\link[tibble:tibble]{tibble} from \code{\link[=fetch_sigmas]{fetch_sigmas()}}, or \code{NULL} on failure.}
\item{ofv}{Numeric from \code{\link[=fetch_ofv]{fetch_ofv()}}.}
\item{condn}{Numeric from \code{\link[=fetch_condn]{fetch_condn()}}, or \code{NA} if no covariance step.}
}
}
\description{
Convenience wrapper that runs all \verb{fetch_*} functions in a single call and
returns a named list. Failed individual parsers produce a warning and return
\code{NULL} for that element rather than stopping the entire call.
}
\examples{
path   <- system.file("testdata", "full_cov.lst", package = "lstparsR")
lst    <- read_lst_file(path)
result <- fetch_all(lst)
result$thetas
result$ofv
}
