% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fetch_ofv.R
\name{fetch_ofv}
\alias{fetch_ofv}
\title{Extract the Objective Function Value (OFV) from a NONMEM Listing File}
\usage{
fetch_ofv(lst, digits = NA)
}
\arguments{
\item{lst}{An object of class \code{"lst"} from \code{\link[=read_lst_file]{read_lst_file()}}.}

\item{digits}{Integer or \code{NA}. Rounding for the OFV. Default \code{NA}.}
}
\value{
A single numeric value (the OFV), or \code{NA_real_} if not found.
}
\description{
Extracts the OFV reported on the \verb{#OBJV:} line of a NONMEM \code{.lst} file.
For failed or early-terminated runs where \verb{#OBJV:} is absent, the function
falls back to the workflow footer (\code{OFV = ...}) and returns \code{NA} with a
warning if neither is found.
}
\examples{
path <- system.file("testdata", "full_cov.lst", package = "lstparsR")
lst  <- read_lst_file(path)
fetch_ofv(lst)
}
